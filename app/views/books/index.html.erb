<p id="notice"><%= notice %></p>

<%= link_to 'New Book', new_book_path, class: "btn btn-primary btn-large pull-right" %>

<h1>Books</h1>

<table class="table">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Publisher</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td>
          <%= link_to image_tag(book.cover_url, alt: book.title, class: "thumbnail-sm"), book, class: "thumbnail" %>
        </td>
        <td><%= link_to book.title, book %></td>
        <td><%= book.publisher %></td>
        <td class="centered">
          <%= link_to edit_book_path(book) do %>
            <span class="glyphicon glyphicon-pencil"> Edit</span>
          <% end %>
        </td>
        <td class="centered">
          <%= link_to book, method: :delete, data: { confirm: 'Are you sure?' } do %>
            <span class="glyphicon glyphicon-trash"> Destroy</span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

